#!/usr/bin/osascript

on run argv
  tell application "iTerm"
    activate
    tell current session of current terminal
      write text "vim " & (argv as text)
    end tell
  end tell
end run

# vim: ft=applescript:
